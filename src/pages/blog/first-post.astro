---
import BaseLayout from "../../layouts/BaseLayout.astro";
const base = "/personal-site/";
---

<BaseLayout title="示例博文 · Matt">
  <article style="max-width:680px;margin:0 auto;">
    <p style="font-family:var(--serif);font-size:13px;color:var(--accent);letter-spacing:0.08em;margin-bottom:8px;">2026.05.30</p>
    <h1 style="font-family:var(--serif);font-size:36px;font-weight:900;line-height:1.25;margin-bottom:32px;">示例博文：第一篇文章</h1>

    <p style="line-height:2;margin-bottom:20px;color:var(--text-muted);">
      这是你的第一篇日志正文。在这里写你想写的内容。
    </p>
    <p style="line-height:2;margin-bottom:20px;color:var(--text-muted);">
      后面可以用 Markdown 格式来写，Astro 会自动渲染。
    </p>

    <a href={`${base}blog`} style="font-family:var(--serif);font-size:13px;color:var(--accent);text-decoration:none;letter-spacing:0.04em;">← 返回日志</a>
  </article>
</BaseLayout>
